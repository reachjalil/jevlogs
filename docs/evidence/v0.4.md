# v0.4 verification — 2026-09-17

- `pnpm test`: 38 tests, 37 pass, 1 skipped (live inference; no `AI_GATEWAY_API_KEY` in the task environment). `pnpm check:examples` passes, including `examples/pager.ts`.
- New API: `createJevPager().decide()`, `shouldPage()`, `JevPagerExporter`, CLI `--page`, config `intent: "page"`.
- Policy encoded in tests: ERROR is scored (not auto-paged); unavailable does not page; `page = probability >= pageAbove`; discrete `urgency` is not an input.
- Not published to npm in this change. Not verified: live Jev pager quality (that measurement is the PagerDuty bake-off JSONL on Hugging Face).
