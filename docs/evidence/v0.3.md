# v0.3 verification — 2026-09-16

- `pnpm test`: 33 tests, 32 pass, 1 skipped (live inference; no `AI_GATEWAY_API_KEY` in the task environment). `pnpm check:examples` and the Astro site build pass.
- End-to-end with the built CLI: `jevlogs.config.json` with `forwardUrl`, a `retain` rule and cache; a stand-in collector on 127.0.0.1:4320; one OTLP JSON batch of three records. Result: health check retained by rule with no model call, ERROR protected, third record failed open as `unavailable` (synthetic key), all three forwarded with `jev.*` attributes, `GET /stats` consistent with stdout, Ctrl+C summary printed.
- Gateway path: a direct `experimental_evaluate` call with a synthetic key returns `GatewayAuthenticationError` 401 from Vercel AI Gateway, confirming model id resolution and auth wiring. The installed `ai@7.0.105` type of `evaluate` matches the fields the SDK reads (`answers.value.score`, `answers.priority.choice`, `answers.actionable.probability`, `usage.inputTokens`).
- Not verified: live Jev answer quality, latency, and billing. Run `pnpm test` with a real key to execute `test/live.test.mjs`.
