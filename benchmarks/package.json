{
  "name": "jevlogs-log-triage-benchmark",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "description": "Reproducible Jev Logs evaluation on Loghub-derived HDFS and BGL samples. Tests the local 0.3.0 build at ../dist/index.js.",
  "scripts": {
    "start": "node run.mjs",
    "prepare-only": "node run.mjs --prepare",
    "pilot": "node run.mjs --pilot",
    "metrics-only": "node run.mjs --metrics-only"
  },
  "dependencies": {
    "ai": "7.0.105"
  }
}
